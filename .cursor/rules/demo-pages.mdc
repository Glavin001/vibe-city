---
globs: src/app/**/page.tsx,src/app/page.tsx
alwaysApply: false
---
- Whenever you add a new page, also add its listing to the root homepage at `src/app/page.tsx`
- Use your browser tool to preview and test the page after making any changes (assume running on http://localhost:3000/ or ask user to start dev server)
- Add StatsGl component (e.g. `<StatsGl className="absolute top-4 left-4" />`) from "@react-three/drei" to all React-Three-Fiber scenes unless explicitly asked not to
